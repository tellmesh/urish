version: '1.0'
project:
  name: urish
  type: [python]
  description: 'urish — TellMesh URI package'
versioning:
  strategy: semver
  files: [pyproject.toml:version]
git:
  commit:
    strategy: conventional
    scope: urish
  tag:
    enabled: true
    prefix: v
strategies:
  python:
    test: pytest tests/ -q
    build: python -m build
    publish: twine upload dist/urish-{version}*
    publish_enabled: true
registries:
  pypi:
    url: https://pypi.org/simple/
    token_env: PYPI_TOKEN
